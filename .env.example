# Base Sepolia private key with ETH for gas (MockUSD is minted on the fly)
# Optional — write-path integration tests are skipped when not set
# External agents can pass this via: ODDMAKI_TEST_PRIVATE_KEY=0x... pnpm run test:live
ODDMAKI_TEST_PRIVATE_KEY=0x...
